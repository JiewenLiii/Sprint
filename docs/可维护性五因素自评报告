# 可维护性五因素自评报告
本次自评针对基于 C++ 开发的控制台 Roguelike 游戏代码库（核心模块包含 Entity、Player、Enemy、Map、Battle、Renderer、Game 等），围绕可理解性、可修改性、可测试性、可靠性、可移植性 五大可维护性核心因素展开评估，分析现状、优势、不足并提出改进建议。
**评估对象**：地牢探险游戏（Dungeon Adventure Game）重构版  
**评估标准**：教材 §8.4.1 可维护性五因素（1-5分制，1=极差，5=优秀）  
**评估日期**：2026年4月20日

## 一、自评得分总览

| 可维护性因素 | 自评分数 | 说明 |
|------------|---------|------|
| 可理解性   | 5       | 代码注释完善、命名规范、模块职责单一、有完整文档（README、DDS、重构报告） |
| 可测试性   | 3       | 缺少单元测试框架，核心逻辑与输入输出强耦合，难以自动化测试 |
| 可修改性   | 4       | 低耦合模块化设计，常量集中定义，修改局部功能影响范围小 |
| 可移植性   | 5       | 已实现跨平台支持（Windows/Linux/macOS），使用条件编译和ANSI颜色 |
| 可重用性   | 3       | 模块间依赖具体类型，未设计为独立库，无法直接在其他项目中复用 |

## 二、低于3分因素的改进方案

### 2.1 可测试性（当前3分）

**问题分析**：

- 游戏逻辑（`Game`类）直接依赖`Keyboard::getKey()`阻塞输入和控制台输出，无法模拟输入序列或捕获输出进行断言
- `Renderer`类直接调用`std::cout`，无法将输出重定向到测试字符串流
- `Enemy::moveTowardsPlayer`依赖随机数（`std::rand`），导致行为不可预测，难以编写确定性测试
- 所有模块紧耦合于具体实现，没有依赖注入或接口抽象

**改进方案**：

1. **方案1：引入输入/输出接口抽象（优先级最高）**
   - 定义`IInputDevice`接口，包含`getKey()`和`keyAvailable()`方法
   - 定义`IOutputDevice`接口，包含`print()`、`setColor()`、`resetColor()`、`clearScreen()`方法
   - 生产环境使用`Keyboard`和`ConsoleOutput`实现这些接口
   - 测试环境使用`MockInput`（可预设按键序列）和`StringOutput`（将输出捕获到字符串流中），从而实现对输入输出的模拟和断言

2. **方案2：为随机行为注入可控随机数生成器**
   - 定义`IRandomGenerator`接口，提供`getInt(min, max)`方法
   - 生产环境使用基于`std::rand`的实现
   - 测试环境使用`FixedRandomGenerator`，返回固定数值序列，使敌人移动行为完全可预测

3. **方案3：为核心类编写单元测试**
   - 选用Google Test或Catch2框架
   - 针对`Map`：测试边界判断、`isWalkable`、视野计算等
   - 针对`Enemy`：测试追击逻辑（当玩家在追击半径内时，敌人应朝玩家移动；当邻接玩家时不应移动，因为应触发战斗）
   - 针对`Battle`：测试伤害计算（最小伤害为1）、回合制流程（先手方攻击后后手方反击）
   - 测试时需提供预置地图（如全空地地图）和固定随机数生成器，确保结果可重复

4. **方案4：分离游戏逻辑与输入输出循环**
   - 将`Game::run()`中的主循环重构为状态机模式
   - 游戏逻辑接收输入事件（枚举类型）并返回渲染数据（如字符串或结构体），不再直接调用I/O
   - 测试时可以直接调用状态转换函数，传入模拟输入事件，然后断言返回的渲染数据是否符合预期，彻底解耦I/O

**预期效果**：改造后可测试性可提升至4分。

### 2.2 可重用性（当前3分）

**问题分析**：

- 各模块（`Map`、`Player`、`Enemy`、`Battle`、`Renderer`）之间存在直接的类型依赖，无法独立于游戏上下文使用
- `Renderer`硬编码了游戏特定的符号（`@`、`E`、`*`、`,`）和颜色映射，无法渲染其他类型的实体
- `Battle`类虽然逻辑独立，但直接接收`Player`和`Enemy`具体类型，无法用于其他`Entity`子类的战斗
- 没有提供稳定的API接口，所有功能都暴露为具体类的方法

**改进方案**：

1. **方案1：定义纯抽象接口，依赖倒置**
   - 定义`ICombatant`接口，包含`getAttack()`、`takeDamage()`、`isAlive()`、`getName()`等方法
   - 将`Battle::executeCombat`的参数改为`ICombatant&`类型（两个战斗单位），使任何实现了该接口的类（如玩家、敌人、NPC）都能参与战斗
   - `Player`和`Enemy`类实现`ICombatant`接口

2. **方案2：将渲染器设计为策略模式，支持自定义符号映射**
   - 定义`ITileRenderer`接口，包含`renderTile()`、`renderEntity()`、`renderFootprint()`等方法
   - 游戏默认提供`DungeonTileRenderer`实现（使用`@`、`E`等符号）
   - 其他项目可提供自己的渲染策略，例如使用Unicode字符或不同颜色方案，无需修改游戏逻辑

3. **方案3：提取可复用的基础库**
   - 将跨平台键盘输入（`Keyboard`类）独立为静态库，供任何控制台游戏复用
   - 将跨平台颜色输出（`Colors`类）独立为静态库，支持ANSI和Windows API的统一封装
   - 将圆形视野算法（欧几里得距离判断）提取为独立函数，输入玩家坐标、视野半径、待测点坐标，返回是否可见
   - 设计泛型网格地图基类`GridMap<T>`，封装宽高、边界检查、遍历等通用功能，游戏中的`Map`可继承自`GridMap<char>`

4. **方案4：使用工厂模式解耦对象创建**
   - 当前`spawnEnemies`函数直接`new`出`Enemy`对象，与具体类绑定
   - 定义`IEnemyFactory`接口，提供`create(x, y, id)`方法返回`std::unique_ptr<Enemy>`
   - 游戏默认使用`DungeonEnemyFactory`，未来可替换为其他工厂（如生成不同类型敌人）

**预期效果**：改造后可重用性可提升至4分。

## 三、其他因素的优势说明

### 可理解性（5分）

- **注释覆盖率**：每个类、关键方法都有Doxygen风格注释，说明职责、参数、返回值
- **命名规范**：类名（`Player`、`Enemy`、`Battle`）、方法名（`moveTowardsPlayer`、`executeCombat`）语义明确
- **模块划分**：7个独立类，每个类代码量不超过500行，符合单一职责原则
- **文档齐全**：提供`README.md`、`BUILD.md`、`DDS.md`、`REFACTORING_REPORT.md`、`CODE_REVIEW_REPORT.md`

### 可修改性（4分）

- **低耦合**：`Game`类通过组合使用`Map`、`Player`、`Battle`、`Renderer`，修改战斗逻辑只需改`Battle`
- **常量集中**：地图尺寸（20×20）、视野半径（6）、足迹数量（5）均定义为`static const`
- **扩展点预留**：`Entity`基类的纯虚函数支持添加新敌人类型（如哥布林、巨龙）
- **扣分项**：部分逻辑仍有硬编码（如敌人追击半径5未提取到配置文件）

### 可移植性（5分）

- **操作系统兼容**：使用预处理宏`_WIN32`区分Windows和类Unix系统，键盘输入在Windows调用`_getch()`，在Linux调用`termios`
- **颜色系统**：`Colors`类封装ANSI转义码（Unix）和Windows API，对上层透明
- **编码处理**：`main.cpp`中调用`SetConsoleOutputCP(65001)`设置UTF-8编码
- **无第三方依赖**：仅使用C++标准库，可在任何支持C++11的编译器上编译

## 四、总结与改进优先级

| 优先级 | 因素       | 当前分数 | 目标分数 | 核心改进工作                                   |
|--------|------------|----------|----------|------------------------------------------------|
| P0     | 可测试性   | 3        | 4        | 引入输入/输出接口抽象，添加单元测试框架         |
| P1     | 可重用性   | 3        | 4        | 定义抽象接口，提取可复用基础库                   |
| P2     | 可修改性   | 4        | 5        | 将剩余硬编码值迁移到配置文件（JSON/INI）         |
| -      | 可理解性   | 5        | -        | 保持现状                                       |
| -      | 可移植性   | 5        | -        | 保持现状                                       |

**结论**：当前代码在可理解性和可移植性方面表现优秀，但在可测试性和可重用性上存在明显短板。按照上述方案进行重构，可将整体可维护性评分提升。

# CIVC 四阀门 AI 辅助开发自评审计报告

本报告基于 CIVC 四阀门（约束/告知/验证/纠正）框架，对本次大作业的 AI 辅助开发过程进行自评审计，明确各环节当前状态、存在的薄弱问题，并提出可落地的改进方案，确保 AI 辅助开发的规范性、安全性与高效性。

- **审计对象**：地牢探险游戏大作业  
- **审计框架**：CIVC 四阀门（约束/告知/验证/纠正）  
- **审计日期**：2026/4/26

## 一、约束（Constraint）

### 当前状态

本次大作业的所有源码文件（含 core 核心逻辑文件、ui 界面文件、test 测试文件等）均无访问和修改限制，AI 可直接访问 `src` 目录下全部文件（包括 core 核心业务层、entities 实体管理层、world 场景地图层等），未设置任何沙盒隔离机制，也未划分 AI 可操作与不可操作的文件范围。AI 生成的代码可直接替换或修改原有作业文件。

### 薄弱问题

- 缺乏文件访问与修改约束，无沙盒隔离保护，AI 可能误改大作业的核心逻辑文件（如核心算法、战斗逻辑、伤害公式等），导致作业核心功能失效。
- 无明确的权限划分，AI 可能随意修改作业的整体结构、命名规范，影响作业的完整性和规范性，甚至破坏项目架构、导致无法正常编译运行。

### 改进方案

1. **建立文件访问白名单机制**  
   明确规定 AI 仅可访问和修改 `test` 测试文件、`ui` 界面美化相关文件；锁定 `core`、`entities`、`world`、`input` 等核心逻辑文件，禁止 AI 对其进行任何修改，仅开放只读权限供 AI 理解项目上下文。

2. **搭建简易沙盒环境**  
   将 AI 生成的代码先放入沙盒中运行验证，确认无问题后，再手动同步至作业主文件，避免直接修改原文件带来的风险。

## 二、告知（Inform）

### 当前状态

已向 AI 提供大作业的核心需求、整体功能框架、简单的代码注释及部分关键逻辑说明，同时提供了项目 `AGENTS.md` 文档及所有源码文件。AI 可初步理解作业的核心用途、基本功能和大致代码结构，能够生成贴合作业需求的基础代码，也可通过文档和源码获取项目架构、目录划分等关键信息。

### 薄弱问题

- 提供给 AI 的项目上下文信息不够完整，缺少详细的模块划分、代码规范、变量定义说明及功能逻辑细节，导致 AI 无法精准理解作业的核心设计思路，可能生成与作业原有逻辑不兼容、不符合规范的代码。
- `AGENTS.md` 未添加版本号，未同步作业的迭代修改记录，AI 无法感知作业的最新调整，可能重复生成已修改或淘汰的代码。

### 改进方案

1. **整理详细说明文档**  
   整理作业的详细说明文档，包含模块划分、代码命名规范、变量含义、核心逻辑流程图及关键功能的实现思路，补充到 `AGENTS.md` 中，提供给 AI 作为参考。

2. **文档版本管理**  
   在 `AGENTS.md` 文档开头添加版本号（如 `v1.0`、`v1.1`），新增“版本变更记录”章节。每次对作业进行修改后，同步更新说明文档和变更记录，向 AI 告知修改内容、修改原因，确保 AI 获取的上下文信息与作业最新状态一致。

## 三、验证（Verify）

### 当前状态

AI 生成代码后，主要依靠人工手动验证：运行代码查看是否符合功能需求、是否存在语法错误，仅对核心功能进行简单的手动测试，未覆盖所有代码分支和异常场景。  
同时项目已内置 doctest 单元测试框架，配置了 GitHub Actions CI 流程，可在代码 push 或 pull request 时自动编译项目并执行单元测试，验证 AI 生成代码的可编译性及基础功能正确性。

### 薄弱问题

- 无完善的自动化测试机制，CI 流程仅执行单元测试和编译校验，未添加代码覆盖率检查，无法量化测试强度。
- 手动测试易遗漏语法错误、逻辑漏洞，尤其是边界场景（如异常输入、功能极限值）的测试不足，导致 AI 生成的隐藏 bug 无法被及时发现，影响作业的正确性和稳定性。

### 改进方案

1. **完善自动化测试环境**  
   针对作业的核心功能、关键代码，编写基础的自动化测试用例（如语法校验、功能验证用例），补充单元测试用例，覆盖**常规场景、边界数值、异常输入**三类场景。

2. **添加代码覆盖率检查**  
   在 CI 流程中添加代码覆盖率检查功能，使用 `gcov` 等工具统计测试覆盖率，设置 **80% 的覆盖率门槛**，当代码覆盖率低于 80% 时，禁止合并代码。

3. **设定明确验证标准**  
   AI 生成的代码必须通过所有自动化测试用例，未通过的代码需重新生成或修改，确保 AI 生成代码的正确性和规范性。

## 四、纠正（Correct）

### 当前状态

当 AI 生成的代码出现错误、导致作业功能异常时，主要依靠人工手动删除错误代码、重新编写或让 AI 重新生成，未设置一键回滚机制，也未备份作业的稳定版本。  
同时可依靠 Git 版本控制进行手动回滚操作，通过执行 `git` 回滚命令恢复至上一版本，无专门的自动化回滚工具或脚本。

### 薄弱问题

- 无高效的错误纠正机制，手动删除、重新编写错误代码耗时较长，且容易误删正确代码。
- 手动回滚操作繁琐，需要团队成员熟悉 Git 命令，且回滚过程中可能误操作，无法快速恢复项目至稳定版本。
- 未备份稳定版本，若错误代码覆盖了原有正确代码，无法快速恢复，影响作业的开发进度和完整性。

### 改进方案

1. **定期备份稳定版本**  
   每次应用 AI 生成的代码前，先备份当前稳定版本（例如使用标签或复制目录）。

2. **编写 Git 自动化回滚脚本**  
   脚本中预设稳定版本标识，包含版本校验、回滚确认、回滚后测试等步骤，实现**一键恢复至最近的稳定版本**，减少人工操作，提高错误纠正效率。

3. **明确纠正流程**  
   AI 生成的代码出错后，先回滚至稳定版本，再告知 AI 错误原因，让其重新生成符合要求的代码，确保改进方案可落地、可执行。

# 地牢探险游戏 - AGENTS.md

## 一、项目架构概述

本文档遵循 **Anthropic 渐进式披露范式**，由宏观架构至微观代码规范逐层披露，供陌生AI编程助手快速理解项目全貌、业务规则、模块划分与开发约束，可直接依据本文档生成规范、可编译、符合项目设计逻辑的C++代码。

本项目为基于C++标准库开发的跨平台控制台地牢探险游戏，支持Windows/Linux/macOS，采用标准面向对象编程思想与分层模块化架构，整体实现高内聚、低耦合的工程化设计。项目将核心战斗逻辑、实体对象管理、地牢地图系统、终端UI渲染、输入系统、自动化单元测试完全分层解耦，各模块职责独立、边界清晰、互不耦合依赖。

项目内置 doctest 单元测试框架，面向所有核心业务API、工具函数、临界分支逻辑编写自动化测试用例，支持功能验证、回归测试与代码覆盖率检测，保障代码稳定性与健壮性。
核心业务包含：双难度选择(Easy/Hard)、20×20地牢地图探索、动态圆形视野迷雾系统、玩家/敌人独立足迹追踪、实体属性管理、回合制战斗结算、标准化伤害计算、战斗日志存储、终端彩色可视化渲染整套闭环玩法，架构扩展性强，支持后续功能迭代开发。

## 二、全局目录结构说明

项目所有源代码、头文件、测试文件统一存放于根目录 `src` 下，目录结构固定锁定，禁止随意新增、删除、移动、重命名文件夹及核心代码文件，保证项目结构统一规范。

### 完整目录划分

- `src/core`：项目核心业务层，承载游戏流程调度、双难度配置、战斗逻辑、回合调度、伤害计算、战斗日志管理、全局常量配置，是项目运行的核心中枢。
- `src/entities`：实体管理层，包含顶层抽象基类 `Entity`，以及派生实体 `Player` 玩家类、`Enemy` 敌人类，统一管理所有游戏生命体的属性、行为、状态与足迹系统。
- `src/world`：场景地图层，负责20×20固定尺寸地牢生成、坐标合法性校验、墙体与空地判定、6格圆形视野计算、迷雾遮挡、探索区域标记、地块可通行性判断、足迹渲染。
- `src/ui`：视图渲染层，纯展示层模块，不包含任何业务逻辑与数据修改操作；负责跨平台终端适配、ANSI颜色控制、终端清屏、地图绘制、战斗界面、玩家状态面板渲染。
- `src/input`：输入系统层，实现跨平台无缓冲键盘监听，支持WASD实时移动操作，无需回车确认。
- `src/test`：自动化测试层，统一存放基于doctest框架编写的单元测试代码，按模块拆分测试文件，覆盖战斗、实体、地图、日志、工具类等全部核心功能。

## 三、核心模块详细职责与关键规则

### 1. Entity 抽象基类

作为项目所有生命体的顶层抽象父类，禁止直接实例化，所有角色、敌人必须公有继承此类。
统一封装基础属性：二维坐标、当前生命值、基础攻击力、存活状态、足迹历史队列；
统一提供通用核心接口：`takeDamage()` 受伤扣血方法、`isAlive()` 存活状态判定方法、属性初始化与重置接口、足迹更新接口；
统一约束实体基础行为，规范全体生命体的代码设计标准。

### 2. Player 玩家类

继承自 Entity 抽象基类，为项目唯一可手动操控实体；
固定初始属性：初始生命值 20，基础攻击力 5，固定数值统一由常量管理，禁止硬编码修改；
核心功能：接收WASD无缓冲移动指令、坐标碰撞检测、行走足迹记录与渐变渲染、状态实时刷新、主动触发地图交互与敌人战斗事件。

### 3. Enemy 敌人类

继承自 Entity 抽象基类，为地牢场景敌对单位；
配置独立战斗属性、终端专属显示符号与名称标识，支持双难度差异化数值配置；
核心AI逻辑：距离玩家≤6格时追击玩家，距离＞6格时随机巡逻，自动规避墙体与其他敌人；被动承接战斗伤害、参与回合战斗结算。

### 4. Battle 战斗模块

全局唯一回合战斗调度核心，管控完整对战流程；
强制统一伤害计算公式：`最终伤害 = max(1, 攻击者攻击力)`，永久保底1点伤害，杜绝零伤害、负伤害异常；
先手规则：玩家主动接触敌人→玩家先手；敌人主动接触玩家→敌人先手；
自动记录战斗过程文本日志，日志最大存储容量固定为10条，超出容量自动淘汰最早日志内容。

### 5. Map 地图模块

固定规格：全局统一 20×20 正方形地牢地图，四周边界全部为不可通行墙体；
核心机制：玩家固定圆形视野半径为6格，未探索区域启用迷雾隐藏，已探索区域永久保留可视状态；
负责坐标越界检测、墙体障碍物阻挡判断、空地通行判定、探索标记更新、玩家/敌人足迹渲染。

### 6. Renderer 渲染模块

纯工具型视图类，只读不写，不修改任何游戏业务数据；
适配跨平台终端特性，使用ANSI颜色码实现彩色渲染、一键清屏、全景地图绘制、战斗界面排版、玩家/敌人状态信息展示、足迹渐变效果渲染，保障终端界面整洁流畅。

### 7. Input 输入模块

跨平台无缓冲键盘输入系统，Windows使用\_kbhit()/\_getch()，Linux/macOS使用termios；
实时监听W/A/S/D移动按键，无延迟响应玩家操作，为游戏核心交互入口。

## 四、全局编码规范约束

1. 命名规范
   类名、抽象基类、结构体统一使用大驼峰命名；成员函数、全局函数、局部变量、成员变量统一使用小驼峰命名；所有命名语义化、见名知意，禁止拼音、缩写、无意义字符命名。

2. 头文件规范
   所有 `.h` 头文件必须添加 `#ifndef / #define / #endif` 头文件宏防护，避免重复包含编译错误；严格分离声明与实现，头文件仅存放声明，业务逻辑实现统一写在 `.cpp` 文件。

3. 平台适配规范
   所有终端清屏、颜色控制、输入监听等平台专属代码，必须使用条件编译隔离；核心算法、战斗逻辑、地图计算保持跨平台通用性，不掺杂系统专属语法。

4. 单元测试规范
   新增任意核心函数、API接口、业务逻辑时，必须同步编写doctest单元测试用例；测试需覆盖常规场景、边界数值、异常输入三类场景，持续保障核心模块测试覆盖率达标。

5. 数值编写规范
   地图尺寸、视野范围、日志上限、角色初始属性、伤害规则、难度数值等固定参数，全部定义为全局常量统一管理；禁止在业务代码中直接书写魔法数字。

6. 代码注释与格式
   统一缩进排版，代码逻辑分段整洁；核心判断、特殊规则、关键算法必须添加简明注释，提升代码可读性，便于AI协作开发与后期维护。

## 五、禁止操作清单

1. 禁止 core、entities、world、ui、input 五大核心模块跨层双向调用，杜绝循环依赖，破坏分层架构。
2. 禁止删除、修改、注释、屏蔽项目现有已完成的单元测试用例，维护自动化测试体系完整性。
3. 禁止擅自修改终端渲染、系统适配、输入监听、底层工具类基础代码，防止程序运行崩溃、界面错乱。
4. 禁止无注释、无说明、无同步沟通的情况下，篡改伤害公式、回合规则、地图机制、实体属性、AI逻辑等核心业务逻辑。
5. 禁止在业务代码中硬编码固定数值，所有全局配置参数必须引用项目统一常量。
6. 禁止修改类继承关系、抽象基类结构、接口定义，禁止私自重构项目底层架构。
7. 禁止随意新增目录、修改文件存放路径、变更原有代码文件命名规范。
8. 禁止生成冗余代码、重复封装工具函数、无效变量与多余头文件引用，保持项目代码简洁轻量化。

## 六、AI 辅助开发指引

所有AI生成代码必须严格遵循本文档架构设计、目录划分、命名规则、接口规范与数值约束；新增功能需匹配现有模块职责，不跨模块堆砌逻辑；如需新增类与方法，严格遵循现有OOP继承体系；输出代码需完整可编译、语法规范、逻辑闭环，完全适配当前项目C++标准库编译环境，支持跨平台运行。
